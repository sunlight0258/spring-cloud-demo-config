spring:
  profiles:
    active:
      - dev

---
spring:
  profiles: dev
  application:
    name: microservice-cloud-config-agile-dev

---
spring:
  profiles: test
  application:
    name: microservice-cloud-config-agile-test
# 请UTF-8 保存
